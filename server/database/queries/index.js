const insertDataQuery = require('./insertData');
const signInQuery = require('./signInQuery');
const insertLoremQuery = require('./insertLorem')

module.exports = { insertDataQuery, signInQuery, insertLoremQuery };